# Prompt 3.19 — Refactor Settings Desktop

## File yang Diubah
`desktop/src/js/settings.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const settings = await window.api.settings.getAll();
const value = await window.api.settings.getByKey(key);
await window.api.settings.save(key, value);
await window.api.settings.reset();

// SESUDAH
const settings = await apiClient.get('/settings');
const value = await apiClient.get(`/settings/${key}`);
await apiClient.put(`/settings/${key}`, { value });
await apiClient.post('/settings/reset');
```

## Hapus dari main.js & preload.js
- `settings:*` handlers

## Output yang Diharapkan
Halaman pengaturan toko berfungsi via API backend.
