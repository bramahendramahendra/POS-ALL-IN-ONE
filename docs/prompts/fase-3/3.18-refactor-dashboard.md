# Prompt 3.18 — Refactor Dashboard Desktop

## File yang Diubah
`desktop/src/js/dashboard.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const stats = await window.api.dashboard.getStats();
const salesTrend = await window.api.dashboard.getSalesTrend(period);
const topProducts = await window.api.dashboard.getTopProducts(limit);
const topCategories = await window.api.dashboard.getTopCategories(limit);
const paymentMethods = await window.api.dashboard.getPaymentMethods();

// SESUDAH
const stats = await apiClient.get('/dashboard/stats');
const salesTrend = await apiClient.get('/dashboard/sales-trend', { period });
const topProducts = await apiClient.get('/dashboard/top-products', { limit });
const topCategories = await apiClient.get('/dashboard/top-categories', { limit });
const paymentMethods = await apiClient.get('/dashboard/payment-methods');
```

## Hapus dari main.js & preload.js
- `dashboard:*` handlers

## Output yang Diharapkan
Halaman dashboard menampilkan data real-time via API backend.
