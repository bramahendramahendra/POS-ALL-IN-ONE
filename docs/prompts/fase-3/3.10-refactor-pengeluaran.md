# Prompt 3.10 — Refactor Pengeluaran Desktop

## File yang Diubah
`desktop/src/js/finance.js` (bagian pengeluaran), `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const expenses = await window.api.expenses.getAll(filters);
const expense = await window.api.expenses.getById(id);
await window.api.expenses.create(data);
await window.api.expenses.update(id, data);
await window.api.expenses.delete(id);

// SESUDAH
const expenses = await apiClient.get('/expenses', { start_date, end_date, category, page, limit });
const expense = await apiClient.get(`/expenses/${id}`);
await apiClient.post('/expenses', data);
await apiClient.put(`/expenses/${id}`, data);
await apiClient.delete(`/expenses/${id}`);
```

## Hapus dari main.js & preload.js
- `expenses:*` handlers

## Output yang Diharapkan
Tab pengeluaran di halaman keuangan berfungsi via API backend.
