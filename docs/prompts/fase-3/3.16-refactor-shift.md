# Prompt 3.16 — Refactor Shift Desktop

## File yang Diubah
`desktop/src/js/shifts.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const shifts = await window.api.shifts.getAll(filters);
const shift = await window.api.shifts.getById(id);
const active = await window.api.shifts.getActive();
await window.api.shifts.start(data);
await window.api.shifts.end(id, data);
const summary = await window.api.shifts.getSummary(id);

// SESUDAH
const shifts = await apiClient.get('/shifts', { start_date, end_date, user_id, page, limit });
const shift = await apiClient.get(`/shifts/${id}`);
const active = await apiClient.get('/shifts/active');
await apiClient.post('/shifts/start', data);
await apiClient.post(`/shifts/${id}/end`, data);
const summary = await apiClient.get(`/shifts/${id}/summary`);
```

## Hapus dari main.js & preload.js
- `shifts:*` handlers

## Output yang Diharapkan
Halaman shift berfungsi via API backend.
