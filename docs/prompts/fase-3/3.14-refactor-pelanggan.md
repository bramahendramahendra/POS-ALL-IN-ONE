# Prompt 3.14 — Refactor Pelanggan Desktop

## File yang Diubah
`desktop/src/js/customers.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const customers = await window.api.customers.getAll(filters);
const activeList = await window.api.customers.getActiveList();
const customer = await window.api.customers.getById(id);
await window.api.customers.create(data);
await window.api.customers.update(id, data);
await window.api.customers.delete(id);
await window.api.customers.toggleStatus(id);

// SESUDAH
const customers = await apiClient.get('/customers', { search, is_active, page, limit });
const activeList = await apiClient.get('/customers/active');
const customer = await apiClient.get(`/customers/${id}`);
await apiClient.post('/customers', data);
await apiClient.put(`/customers/${id}`, data);
await apiClient.delete(`/customers/${id}`);
await apiClient.patch(`/customers/${id}/toggle-status`);
```

## Hapus dari main.js & preload.js
- `customers:*` handlers

## Output yang Diharapkan
Halaman pelanggan berfungsi via API backend.
