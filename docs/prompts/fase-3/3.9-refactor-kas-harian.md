# Prompt 3.9 — Refactor Kas Harian Desktop

## File yang Diubah
`desktop/src/js/my-cash.js`, `desktop/src/js/finance.js` (bagian kas), `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const current = await window.api.cashDrawer.getCurrent();
await window.api.cashDrawer.open({ shift_id, opening_balance });
await window.api.cashDrawer.close(id, { closing_balance, notes });
const history = await window.api.cashDrawer.getHistory(filters);
const detail = await window.api.cashDrawer.getById(id);

// SESUDAH
const current = await apiClient.get('/cash-drawer/current');
await apiClient.post('/cash-drawer/open', { shift_id, opening_balance });
await apiClient.post(`/cash-drawer/${id}/close`, { closing_balance, notes });
const history = await apiClient.get('/cash-drawer', { start_date, end_date, user_id, page, limit });
const detail = await apiClient.get(`/cash-drawer/${id}`);
```

## Hapus dari main.js & preload.js
- `cashDrawer:*` handlers

## Output yang Diharapkan
Halaman kas saya dan bagian kas di keuangan berfungsi via API backend.
