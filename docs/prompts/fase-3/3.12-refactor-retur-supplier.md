# Prompt 3.12 — Refactor Retur Supplier Desktop

## File yang Diubah
`desktop/src/js/suppliers.js` (bagian retur), `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const returns = await window.api.supplierReturns.getAll(filters);
const ret = await window.api.supplierReturns.getById(id);
await window.api.supplierReturns.create(data);
await window.api.supplierReturns.updateStatus(id, status);
await window.api.supplierReturns.delete(id);
const items = await window.api.supplierReturns.getPurchaseItems(purchaseId);

// SESUDAH
const returns = await apiClient.get('/supplier-returns', { start_date, end_date, supplier_id, status, page, limit });
const ret = await apiClient.get(`/supplier-returns/${id}`);
await apiClient.post('/supplier-returns', data);
await apiClient.patch(`/supplier-returns/${id}/status`, { status, notes });
await apiClient.delete(`/supplier-returns/${id}`);
const items = await apiClient.get(`/purchases/${purchaseId}/items`);
```

## Hapus dari main.js & preload.js
- `supplierReturns:*` handlers

## Output yang Diharapkan
Bagian retur supplier berfungsi via API backend.
