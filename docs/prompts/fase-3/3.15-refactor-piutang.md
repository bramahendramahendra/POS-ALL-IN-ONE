# Prompt 3.15 — Refactor Piutang Desktop

## File yang Diubah
`desktop/src/js/receivables.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const receivables = await window.api.receivables.getAll(filters);
const receivable = await window.api.receivables.getById(id);
const summary = await window.api.receivables.getSummaryByCustomer();
await window.api.receivables.pay(id, paymentData);
const payments = await window.api.receivables.getPayments(id);

// SESUDAH
const receivables = await apiClient.get('/receivables', { customer_id, status, start_date, end_date, page, limit });
const receivable = await apiClient.get(`/receivables/${id}`);
const summary = await apiClient.get('/receivables/summary');
await apiClient.post(`/receivables/${id}/pay`, paymentData);
const payments = await apiClient.get(`/receivables/${id}/payments`);
```

## Hapus dari main.js & preload.js
- `receivables:*` handlers

## Output yang Diharapkan
Halaman piutang berfungsi via API backend.
