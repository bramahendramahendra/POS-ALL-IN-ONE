# Prompt 3.11 — Refactor Purchase Order Desktop

## File yang Diubah
`desktop/src/js/finance.js` (bagian pembelian), `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const purchases = await window.api.purchases.getAll(filters);
const purchase = await window.api.purchases.getById(id);
await window.api.purchases.create(data);
await window.api.purchases.update(id, data);
await window.api.purchases.delete(id);
await window.api.purchases.pay(id, paymentData);

// SESUDAH
const purchases = await apiClient.get('/purchases', { start_date, end_date, supplier_id, payment_status, page, limit });
const purchase = await apiClient.get(`/purchases/${id}`);
await apiClient.post('/purchases', data);
await apiClient.put(`/purchases/${id}`, data);
await apiClient.delete(`/purchases/${id}`);
await apiClient.post(`/purchases/${id}/pay`, paymentData);
```

## Hapus dari main.js & preload.js
- `purchases:*` handlers

## Output yang Diharapkan
Tab pembelian di halaman keuangan berfungsi via API backend.
