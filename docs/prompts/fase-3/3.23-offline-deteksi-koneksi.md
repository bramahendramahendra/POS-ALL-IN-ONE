# Prompt 3.23 — Offline Mode: Deteksi Koneksi

## File Baru / Diubah
`desktop/src/js/connection-monitor.js`, `desktop/src/js/app.js` (atau main entry point)

## Implementasi

```javascript
// connection-monitor.js
class ConnectionMonitor {
    constructor() {
        this.isOnline = navigator.onLine;
        this.backendUrl = window.APP_CONFIG?.backendUrl || 'http://localhost:8080';
        this.checkInterval = null;
        this.listeners = [];
    }

    start() {
        // Deteksi perubahan koneksi browser
        window.addEventListener('online', () => this._handleChange(true));
        window.addEventListener('offline', () => this._handleChange(false));

        // Ping backend setiap 30 detik untuk cek koneksi real
        this.checkInterval = setInterval(() => this._pingBackend(), 30000);
        this._pingBackend(); // cek awal
    }

    async _pingBackend() {
        try {
            const res = await fetch(`${this.backendUrl}/health`, {
                method: 'GET',
                signal: AbortSignal.timeout(3000) // timeout 3 detik
            });
            this._handleChange(res.ok);
        } catch {
            this._handleChange(false);
        }
    }

    _handleChange(online) {
        const changed = this.isOnline !== online;
        this.isOnline = online;
        this._updateUI();
        if (changed) {
            this.listeners.forEach(fn => fn(online));
            if (online) {
                // Trigger antrian sync saat baru online kembali
                window.dispatchEvent(new CustomEvent('connection:restored'));
            }
        }
    }

    _updateUI() {
        const indicator = document.getElementById('connection-indicator');
        if (!indicator) return;
        indicator.className = this.isOnline ? 'status-online' : 'status-offline';
        indicator.textContent = this.isOnline ? '● Online' : '● Offline';
    }

    onChange(fn) {
        this.listeners.push(fn);
    }

    stop() {
        clearInterval(this.checkInterval);
    }
}

const connectionMonitor = new ConnectionMonitor();
connectionMonitor.start();
```

## HTML — Indikator di Layout Utama

```html
<!-- Tambahkan di header/navbar -->
<span id="connection-indicator" class="status-online">● Online</span>
```

```css
.status-online  { color: #22c55e; font-size: 12px; }
.status-offline { color: #ef4444; font-size: 12px; }
```

## Integrasi dengan API Client

```javascript
// Di api-client.js — cek status sebelum request
async request(method, endpoint, data, options) {
    if (!connectionMonitor.isOnline) {
        // Alihkan ke SQLite lokal (akan diimplementasi di 3.24 & 3.26)
        throw { offline: true, message: 'Tidak ada koneksi internet' };
    }
    // ... lanjut fetch seperti biasa
}
```

## Output yang Diharapkan
- Indikator koneksi tampil realtime di semua halaman
- Event `connection:restored` terpicu saat kembali online (digunakan oleh Sync Engine di 3.27)
- API Client otomatis tahu status koneksi sebelum melakukan request
