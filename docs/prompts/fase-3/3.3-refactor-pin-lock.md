# Prompt 3.3 — Refactor PIN Lock Desktop

## File yang Diubah
`desktop/src/js/auth.js` (bagian PIN) dan `desktop/main.js`

## Perubahan

### Sebelum (IPC)
```javascript
await window.api.pinLock.hasPin();
await window.api.pinLock.setPin(pin);
await window.api.pinLock.verifyPin(pin);
await window.api.pinLock.changePin(oldPin, newPin);
```

### Sesudah (fetch ke backend)
```javascript
async function hasPin() {
    const data = await apiClient.get('/pin/check');
    return data.has_pin;
}

async function setPin(pin) {
    return await apiClient.post('/pin/set', { pin });
}

async function verifyPin(pin) {
    return await apiClient.post('/pin/verify', { pin });
}

async function changePin(oldPin, newPin) {
    return await apiClient.post('/pin/change', { old_pin: oldPin, new_pin: newPin });
}
```

## Update `desktop/main.js`
Hapus IPC handlers:
- `pinlock:hasPin`
- `pinlock:setPin`
- `pinlock:verifyPin`
- `pinlock:changePin`

## Update `desktop/preload.js`
Hapus expose:
- `pinLock.hasPin`
- `pinLock.setPin`
- `pinLock.verifyPin`
- `pinLock.changePin`

## Output yang Diharapkan
PIN lock berfungsi sama seperti sebelumnya tapi melalui API backend.
