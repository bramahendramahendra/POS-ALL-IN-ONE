# Prompt 3.13 — Refactor Supplier Desktop

## File yang Diubah
`desktop/src/js/suppliers.js`, `desktop/main.js`, `desktop/preload.js`

## Perubahan

```javascript
// SEBELUM
const suppliers = await window.api.suppliers.getAll(filters);
const activeList = await window.api.suppliers.getActiveList();
const supplier = await window.api.suppliers.getById(id);
const detail = await window.api.suppliers.getDetail(id);
await window.api.suppliers.create(data);
await window.api.suppliers.update(id, data);
await window.api.suppliers.delete(id);
await window.api.suppliers.toggleStatus(id);

// SESUDAH
const suppliers = await apiClient.get('/suppliers', { search, is_active, page, limit });
const activeList = await apiClient.get('/suppliers/active');
const detail = await apiClient.get(`/suppliers/${id}`);
await apiClient.post('/suppliers', data);
await apiClient.put(`/suppliers/${id}`, data);
await apiClient.delete(`/suppliers/${id}`);
await apiClient.patch(`/suppliers/${id}/toggle-status`);
```

## Hapus dari main.js & preload.js
- `suppliers:*` handlers

## Output yang Diharapkan
Halaman supplier berfungsi via API backend.
