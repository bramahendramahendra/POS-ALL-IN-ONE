# Prompt 4.1 — Setup Web App & Routing

## Struktur Folder Baru

```
web/
├── index.html              ← Entry point, redirect ke login/dashboard
├── login.html
├── dashboard.html
├── kasir.html
├── products.html
├── transactions.html
├── finance.html
├── customers.html
├── receivables.html
├── suppliers.html
├── shifts.html
├── reports.html
├── settings.html
├── sync-center.html
├── src/
│   ├── js/
│   │   ├── api-client.js   ← Sama persis dengan desktop (copy & share)
│   │   ├── auth.js
│   │   ├── router.js       ← Client-side routing
│   │   └── app.js          ← Entry point JS
│   ├── css/
│   │   └── style.css       ← Shared style dari desktop
│   └── assets/
└── package.json            ← Opsional (jika pakai bundler)
```

## File: src/js/api-client.js
**Salin identik dari `desktop/src/js/api-client.js`** — tidak ada perubahan karena sudah menggunakan `fetch()` standar.

## File: src/js/router.js

```javascript
// Client-side router sederhana tanpa library
const routes = {
    '/login':        'login.html',
    '/dashboard':    'dashboard.html',
    '/kasir':        'kasir.html',
    '/products':     'products.html',
    '/transactions': 'transactions.html',
    '/finance':      'finance.html',
    '/customers':    'customers.html',
    '/receivables':  'receivables.html',
    '/suppliers':    'suppliers.html',
    '/shifts':       'shifts.html',
    '/reports':      'reports.html',
    '/settings':     'settings.html',
    '/sync-center':  'sync-center.html',
};

function navigate(path) {
    const page = routes[path];
    if (!page) { window.location.href = '/login.html'; return; }
    window.location.href = `/${page}`;
}

// Guard: redirect ke login jika belum auth
function requireAuth() {
    const token = localStorage.getItem('access_token');
    if (!token) { navigate('/login'); }
}
```

## File: src/js/app.js

```javascript
// Konfigurasi base URL backend
window.APP_CONFIG = {
    backendUrl: 'https://api.namadomain.com', // ganti sesuai production
    platform: 'web',
};

// Tambahkan platform ke setiap request
// (digunakan di api-client.js untuk header X-Platform)
```

## File: index.html

```html
<!DOCTYPE html>
<html>
<head><meta charset="UTF-8"><title>POS System</title></head>
<body>
<script>
    const token = localStorage.getItem('access_token');
    window.location.href = token ? '/dashboard.html' : '/login.html';
</script>
</body>
</html>
```

## Perbedaan Desktop vs Web
| Aspek | Desktop (Electron) | Web (Browser) |
|---|---|---|
| Base URL | `http://localhost:8080` | `https://api.namadomain.com` |
| Platform header | `X-Platform: desktop` | `X-Platform: web` |
| Offline mode | Ada (SQLite lokal) | Tidak ada |
| Sync Center | Ada | Ada (read-only view) |
| Print struk | Thermal printer | Browser print |
| File dialog | Electron dialog | HTML `<input type="file">` |

## Output yang Diharapkan
Struktur folder web siap, API client terkonfigurasi, routing dasar berfungsi.
