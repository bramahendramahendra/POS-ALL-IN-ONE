# Prompt 2.14 — Domain Retur Supplier

## Struktur
```
domain/supplier_return/
├── handler/handler_supplier_return.go
├── service/service_supplier_return_interface.go
├── service/service_supplier_return.go
├── repo/repo_supplier_return_interface.go
├── repo/repo_supplier_return.go
├── model/model_supplier_return.go
└── dto/dto_supplier_return.go
```

## Raw SQL Penting
```go
const (
    GenerateReturnCodeQuery   = `SELECT COUNT(*) FROM supplier_returns WHERE DATE(return_date) = ?`
    CreateReturnQuery         = `INSERT INTO supplier_returns (return_code, purchase_id, supplier_id, supplier_name, return_date, total_return_amount, reason, status, user_id, notes) VALUES (?, ?, ?, ?, ?, ?, ?, 'pending', ?, ?)`
    CreateReturnItemQuery     = `INSERT INTO supplier_return_items (return_id, purchase_item_id, product_id, product_name, quantity, unit, purchase_price, subtotal) VALUES (?, ?, ?, ?, ?, ?, ?, ?)`
    UpdateReturnStatusQuery   = `UPDATE supplier_returns SET status = ?, notes = ?, updated_at = NOW() WHERE id = ?`
    ReduceStockQuery          = `UPDATE products SET stock = stock - ?, updated_at = NOW() WHERE id = ?`
    GetReturnItemsQuery       = `SELECT sri.id, sri.product_id, sri.product_name, sri.quantity, sri.unit, sri.purchase_price, sri.subtotal FROM supplier_return_items sri WHERE sri.return_id = ?`
    CheckReturnApprovedQuery  = `SELECT status FROM supplier_returns WHERE id = ?`
)
```

## Service Logic — Update Status
```go
func (s *supplierReturnService) UpdateStatus(id int, status, notes string, userID int) error {
    // Cek status saat ini
    current, _ := s.repo.GetStatus(id)
    if current == "approved" {
        return &errors.BadRequestError{Message: "Retur yang sudah approved tidak bisa diubah"}
    }

    if status == "approved" {
        return s.repo.WithTransaction(func(tx *gorm.DB) error {
            // Update status
            // Ambil semua items
            // Kurangi stok setiap item
            // Catat mutasi stok tipe 'return'
        })
    }

    return s.repo.UpdateStatus(id, status, notes)
}
```

## Routes
```go
returnGroup := protected.Group("/supplier-returns")
returnGroup.GET("", returnHandler.GetAll)
returnGroup.GET("/:id", returnHandler.GetByID)
returnGroup.POST("", RoleMiddleware("owner","admin"), returnHandler.Create)
returnGroup.PATCH("/:id/status", RoleMiddleware("owner","admin"), returnHandler.UpdateStatus)
returnGroup.DELETE("/:id", RoleMiddleware("owner","admin"), returnHandler.Delete)
```

## Output yang Diharapkan
Semua endpoint sesuai kontrak di prompt 1.12.
