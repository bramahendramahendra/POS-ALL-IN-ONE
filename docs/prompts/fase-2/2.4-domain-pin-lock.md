# Prompt 2.4 — Domain PIN Lock

## Konteks
PIN digunakan kasir sebagai keamanan tambahan.
Disimpan sebagai bcrypt hash di kolom `pin_hash` tabel `users`.
Ikuti pola domain: handler → service → repo → dto.

## Struktur
```
domain/pin/
├── handler/handler_pin.go
├── service/service_pin_interface.go
├── service/service_pin.go
├── repo/repo_pin_interface.go
├── repo/repo_pin.go
└── dto/dto_pin.go
```

## DTO
```go
type SetPinRequest struct {
    Pin string `json:"pin" validate:"required,numeric,min=4,max=6"`
}

type ChangePinRequest struct {
    OldPin string `json:"old_pin" validate:"required,numeric,min=4,max=6"`
    NewPin string `json:"new_pin" validate:"required,numeric,min=4,max=6"`
}

type VerifyPinRequest struct {
    Pin string `json:"pin" validate:"required,numeric,min=4,max=6"`
}
```

## Repository — Raw SQL
```go
const (
    GetPinHashQuery    = `SELECT pin_hash FROM users WHERE id = ? LIMIT 1`
    SetPinHashQuery    = `UPDATE users SET pin_hash = ? WHERE id = ?`
    ClearPinHashQuery  = `UPDATE users SET pin_hash = NULL WHERE id = ?`
)
```

## Service Logic
```go
func (s *pinService) HasPin(userID int) (bool, error) {
    pinHash, err := s.repo.GetPinHash(userID)
    return pinHash != "", err
}

func (s *pinService) SetPin(userID int, pin string) error {
    hasPin, _ := s.HasPin(userID)
    if hasPin {
        return &errors.BadRequestError{Message: "PIN sudah ada, gunakan endpoint ubah PIN"}
    }
    hashed, _ := bcrypt.Hash(pin)
    return s.repo.SetPinHash(userID, hashed)
}

func (s *pinService) VerifyPin(userID int, pin string) (bool, error) {
    pinHash, err := s.repo.GetPinHash(userID)
    if err != nil { return false, err }
    return bcrypt.Verify(pin, pinHash), nil
}

func (s *pinService) ChangePin(userID int, oldPin, newPin string) error {
    valid, err := s.VerifyPin(userID, oldPin)
    if err != nil { return err }
    if !valid {
        return &errors.UnauthenticatedError{Message: "PIN lama tidak sesuai"}
    }
    hashed, _ := bcrypt.Hash(newPin)
    return s.repo.SetPinHash(userID, hashed)
}
```

## Handler & Routes
```go
// GET  /api/pin/check
// POST /api/pin/set
// POST /api/pin/verify
// POST /api/pin/change
```

Semua endpoint memerlukan Bearer token.
`user_id` diambil dari context: `helper.GetUserID(c)`

## Output yang Diharapkan
Semua endpoint sesuai kontrak di prompt 1.3.
