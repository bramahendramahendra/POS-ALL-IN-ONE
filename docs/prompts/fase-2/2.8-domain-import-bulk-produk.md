# Prompt 2.8 — Domain Import Bulk Produk

## Konteks
Import produk dari file Excel/CSV.
Library `excelize` sudah ada di go.mod — gunakan langsung.

## Handler
Tambahkan di `domain/product/handler/handler_product.go`:

```go
// POST /api/products/import
func (h *ProductHandler) Import(c *gin.Context) {
    file, err := c.FormFile("file")
    if err != nil {
        c.Error(&errors.BadRequestError{Message: "File tidak ditemukan"})
        return
    }

    // Validasi ekstensi
    ext := filepath.Ext(file.Filename)
    if ext != ".xlsx" && ext != ".xls" && ext != ".csv" {
        c.Error(&errors.BadRequestError{Message: "Format file harus .xlsx atau .csv"})
        return
    }

    result, err := h.service.ImportFromFile(file)
    if err != nil {
        c.Error(err)
        return
    }

    response_helper.WrapResponse(c, 200, "json", &global_dto.ResponseParams{
        Code: helper.StatusOk, Status: true, Message: "Import selesai", Data: result,
    })
}
```

## Service Logic
```go
type ImportResult struct {
    Success int                  `json:"success"`
    Failed  int                  `json:"failed"`
    Errors  []ImportErrorDetail  `json:"errors"`
}

type ImportErrorDetail struct {
    Row     int    `json:"row"`
    Message string `json:"message"`
}

func (s *productService) ImportFromFile(file *multipart.FileHeader) (*ImportResult, error) {
    // 1. Buka file dengan excelize
    f, err := excelize.OpenReader(src)

    // 2. Baca sheet pertama
    rows, err := f.GetRows("Sheet1")

    // 3. Skip header row (baris pertama)
    // Kolom: barcode | name | category | purchase_price | selling_price | stock | min_stock | unit

    // 4. Loop setiap baris
    for i, row := range rows[1:] {
        rowNum := i + 2
        // Validasi: name dan selling_price wajib ada
        // Cari category_id by name (buat baru jika belum ada)
        // Cek barcode unik
        // Insert produk
        // Catat error jika gagal
    }

    return result, nil
}
```

## Format Excel yang Diterima
```
| barcode       | name       | category | purchase_price | selling_price | stock | min_stock | unit |
|---------------|------------|----------|----------------|---------------|-------|-----------|------|
| 8991234567890 | Kopi Susu  | Minuman  | 8000           | 12000         | 50    | 10        | pcs  |
```

## Output yang Diharapkan
Response import dengan jumlah sukses, gagal, dan detail error per baris.
