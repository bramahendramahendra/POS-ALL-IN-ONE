# Prompt 2.13 — Domain Purchase Order

## Struktur
```
domain/purchase/
├── handler/handler_purchase.go
├── service/service_purchase_interface.go
├── service/service_purchase.go
├── repo/repo_purchase_interface.go
├── repo/repo_purchase.go
├── model/model_purchase.go
└── dto/dto_purchase.go
```

## Raw SQL Penting
```go
const (
    GeneratePurchaseCodeQuery = `SELECT COUNT(*) FROM purchases WHERE DATE(purchase_date) = ?`
    CreatePurchaseQuery       = `INSERT INTO purchases (purchase_code, supplier_id, supplier_name, purchase_date, total_amount, payment_status, paid_amount, remaining_amount, user_id, notes) VALUES (?, ?, ?, ?, ?, 'unpaid', 0, ?, ?, ?)`
    CreatePurchaseItemQuery   = `INSERT INTO purchase_items (purchase_id, product_id, product_name, quantity, unit, purchase_price, subtotal) VALUES (?, ?, ?, ?, ?, ?, ?)`
    AddStockQuery             = `UPDATE products SET stock = stock + ?, updated_at = NOW() WHERE id = ?`
    PayPurchaseQuery          = `UPDATE purchases SET paid_amount = paid_amount + ?, remaining_amount = remaining_amount - ?, payment_status = CASE WHEN remaining_amount - ? <= 0 THEN 'paid' WHEN paid_amount + ? > 0 THEN 'partial' ELSE 'unpaid' END, updated_at = NOW() WHERE id = ?`
    GetPurchaseItemsQuery     = `SELECT id, product_id, product_name, quantity, unit, purchase_price, subtotal FROM purchase_items WHERE purchase_id = ?`
    RollbackStockQuery        = `UPDATE products SET stock = stock - ?, updated_at = NOW() WHERE id = ?`
)
```

## Service Logic — Create PO
```go
func (s *purchaseService) Create(req *dto.PurchaseRequest, userID int) (*dto.PurchaseResponse, error) {
    return s.repo.WithTransaction(func(tx *gorm.DB) error {
        // 1. Generate purchase_code: PO-YYYYMMDD-XXX
        // 2. Hitung total_amount
        // 3. Simpan PO header
        // 4. Loop items: simpan item + tambah stok + catat mutasi
    })
}
```

## Service Logic — Delete PO
```go
func (s *purchaseService) Delete(id int) error {
    // Cek apakah ada pembayaran
    po, _ := s.repo.GetByID(id)
    if po.PaidAmount > 0 {
        return &errors.BadRequestError{Message: "PO tidak bisa dihapus karena sudah ada pembayaran"}
    }
    return s.repo.WithTransaction(func(tx *gorm.DB) error {
        // Rollback stok semua item
        // Hapus items
        // Hapus PO
    })
}
```

## Routes
```go
purchaseGroup := protected.Group("/purchases")
purchaseGroup.GET("", purchaseHandler.GetAll)
purchaseGroup.GET("/:id", purchaseHandler.GetByID)
purchaseGroup.GET("/:id/items", purchaseHandler.GetItems)
purchaseGroup.POST("", RoleMiddleware("owner","admin"), purchaseHandler.Create)
purchaseGroup.PUT("/:id", RoleMiddleware("owner","admin"), purchaseHandler.Update)
purchaseGroup.DELETE("/:id", RoleMiddleware("owner","admin"), purchaseHandler.Delete)
purchaseGroup.POST("/:id/pay", RoleMiddleware("owner","admin"), purchaseHandler.Pay)
```

## Output yang Diharapkan
Semua endpoint sesuai kontrak di prompt 1.11.
