# Prompt 2.9 — Domain Product Units & Harga Tier

## Konteks
Tambahkan ke domain product yang sudah ada di prompt 2.7.
Product units dan harga tier adalah sub-resource dari produk.

## Tambahkan ke Struktur domain/product/
```
domain/product/
├── handler/
│   ├── handler_product.go       ← sudah ada
│   ├── handler_product_unit.go  ← tambah
│   └── handler_product_price.go ← tambah
├── repo/
│   ├── repo_product_unit.go     ← tambah
│   └── repo_product_price.go   ← tambah
└── dto/
    ├── dto_product_unit.go      ← tambah
    └── dto_product_price.go    ← tambah
```

## Raw SQL — Product Units
```go
const (
    GetProductUnitsQuery  = `SELECT pu.id, pu.product_id, pu.unit_id, pu.unit_name, pu.conversion_qty, pu.selling_price, pu.is_default FROM product_units pu WHERE pu.product_id = ?`
    DeleteProductUnitsQuery = `DELETE FROM product_units WHERE product_id = ?`
    InsertProductUnitQuery  = `INSERT INTO product_units (product_id, unit_id, unit_name, conversion_qty, selling_price, is_default) VALUES (?, ?, ?, ?, ?, ?)`
    DeleteProductUnitByIDQuery = `DELETE FROM product_units WHERE id = ? AND product_id = ?`
)
```

## Raw SQL — Product Prices
```go
const (
    GetProductPricesQuery   = `SELECT id, product_id, tier_name, min_qty, price FROM product_prices WHERE product_id = ? ORDER BY min_qty`
    DeleteProductPricesQuery = `DELETE FROM product_prices WHERE product_id = ?`
    InsertProductPriceQuery  = `INSERT INTO product_prices (product_id, tier_name, min_qty, price) VALUES (?, ?, ?, ?)`
)
```

## Service Logic
```go
// Product Units — Save (replace all)
func (s *productService) SaveProductUnits(productID int, units []dto.ProductUnitRequest) error {
    // 1. Hapus semua unit lama untuk product_id ini
    // 2. Insert unit-unit baru
    // Gunakan transaksi DB agar atomic
}

// Product Prices — Save (replace all)
func (s *productService) SaveProductPrices(productID int, prices []dto.ProductPriceRequest) error {
    // 1. Hapus semua harga tier lama
    // 2. Insert harga tier baru
}
```

## Routes
```go
// Sub-resource dari /products/:product_id
productGroup.GET("/:product_id/units", productUnitHandler.GetByProduct)
productGroup.POST("/:product_id/units", RoleMiddleware("owner","admin"), productUnitHandler.Save)
productGroup.DELETE("/:product_id/units/:unit_id", RoleMiddleware("owner","admin"), productUnitHandler.Delete)

productGroup.GET("/:product_id/prices", productPriceHandler.GetByProduct)
productGroup.POST("/:product_id/prices", RoleMiddleware("owner","admin"), productPriceHandler.Save)
```

## Output yang Diharapkan
Semua endpoint sesuai kontrak di prompt 1.7.
