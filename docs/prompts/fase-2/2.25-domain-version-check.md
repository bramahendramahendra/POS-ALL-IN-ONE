# Prompt 2.25 — Domain Version Check Android

## Struktur
```
domain/version/
├── handler/handler_version.go
├── service/service_version_interface.go
├── service/service_version.go
├── repo/repo_version_interface.go
├── repo/repo_version.go
├── model/model_version.go
└── dto/dto_version.go
```

## Raw SQL
```go
const (
    GetLatestVersionQuery = `SELECT version, download_url, release_notes, is_mandatory FROM app_versions WHERE platform = 'android' AND is_latest = 1 LIMIT 1`
    SetAllNotLatestQuery  = `UPDATE app_versions SET is_latest = 0 WHERE platform = 'android'`
    CreateVersionQuery    = `INSERT INTO app_versions (platform, version, download_url, release_notes, is_mandatory, is_latest) VALUES ('android', ?, ?, ?, ?, 1)`
)
```

## Service Logic
```go
func (s *versionService) CheckAndroid(currentVersion string) (*dto.VersionCheckResponse, error) {
    latest, err := s.repo.GetLatestAndroid()
    if err != nil { return nil, err }

    hasUpdate := latest.Version != currentVersion
    // Bandingkan versi secara semantik jika perlu

    return &dto.VersionCheckResponse{
        LatestVersion:  latest.Version,
        CurrentVersion: currentVersion,
        HasUpdate:      hasUpdate,
        DownloadURL:    latest.DownloadURL,
        ReleaseNotes:   latest.ReleaseNotes,
        IsMandatory:    latest.IsMandatory,
    }, nil
}
```

## Routes
```go
// Public — tidak perlu auth (dipanggil saat app buka sebelum login)
public.GET("/version/android", versionHandler.CheckAndroid)

// Protected — hanya admin
protected.POST("/version/android", RoleMiddleware("admin"), versionHandler.UpdateAndroidVersion)
```

## Output yang Diharapkan
Semua endpoint sesuai kontrak di prompt 1.26.
